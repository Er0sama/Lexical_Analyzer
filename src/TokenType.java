/**
 * Token types recognized by the lexical analyzer
 */
public enum TokenType {
    IDENTIFIER,
    INTEGER,
    FLOAT,
    BOOLEAN,
    COMMENT,
    RELATIONAL,
    OPERATOR,
    
    WHITESPACE,
    EOF,
    ERROR
}
